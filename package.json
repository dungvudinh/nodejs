{
  "name": "tiktok-backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon --inspect ./src/index.js",
    "prettier": "prettier --single-quote --tab-width=4 --trailing-comma all  --write src/**/*.{js,json,scss} ",
    "watch": "node-sass --watch src/resources/scss -o src/public/scss",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "src/**/*.{js,json,scss}": "prettier --single-quote --tab-width=4 --trailing-comma all  --write"
  }, 
  "husky":{
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }, 
  "author": "dungg",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-handlebars": "^7.1.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "node-sass": "^9.0.0",
    "prettier": "^3.0.2"
  }
}
